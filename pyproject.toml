[project]
name = "lean"
version = "1.0.0"
description = "Vault Orchestrator - Windows-native sandboxed AI task runner"
requires-python = ">=3.11"
dependencies = ["httpx>=0.25.0"]

[project.scripts]
lean = "lean.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
